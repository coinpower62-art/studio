This folder is for your manually uploaded assets.

You can add images to this directory and then reference them in your application from the root.

For example, if you add an image named `my-image.png` here, you can use it in your code like this:

`<img src="/attached-asset/my-image.png" alt="My Image" />`
